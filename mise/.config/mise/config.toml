[settings]
idiomatic_version_file_enable_tools = ["python"]

[tools]
bun = "latest"
java = "21"
node = "24"
"npm:pnpm" = "latest"
go = "latest"
lua = "latest"
rust = "stable"
uv = "latest"
ruby = "latest"
pipx = "latest"
gcloud = "latest"
awscli = "latest"
"github:aurokin/clip-remote-sync" = { version = "latest", asset_pattern = "crs", bin = "crs" }
"github:aurokin/agentscan" = "v0.12.1"
# Pinned: mise's minimum_release_age default hides fresh releases from "latest";
# bump the pin when cutting a new trunkyard release.
"github:aurokin/trunkyard" = { version = "v0.1.0", asset_pattern = "trunkyard.tar.gz", bin_path = "trunkyard/bin" }
# Pinned (same minimum_release_age reason as trunkyard); v0.7.0 carries the
# AUR-702 composable-sources contract the tprompt stow package requires.
"github:aurokin/tprompt" = "v0.7.0"
"github:steipete/goplaces" = "latest"
"github:steipete/gogcli" = { version = "latest", bin = "gog" }
"github:steipete/imsg" = { version = "latest", os = ["macos"] }
"go:github.com/xdevplatform/xurl" = "latest"

# Node CLIs
# OpenCode v2 preview; track the upstream beta channel until v2 becomes stable.
# fleet-update force-reinstalls this dist-tag because mise cannot detect tag movement;
# allow its reviewed postinstall so the resulting CLI is usable.
"npm:opencode-ai" = { version = "beta", allow_builds = ["opencode-ai"] }
"npm:@fsouza/prettierd" = "latest"
"npm:@vue/typescript-plugin" = "latest"
"npm:typescript-language-server" = "latest"
"npm:typescript" = "latest"
"npm:@google/gemini-cli" = "latest"
"npm:@github/copilot" = "latest"
"npm:@googleworkspace/cli" = "latest"
"npm:skills" = "latest"
# Pinned (same minimum_release_age reason as trunkyard); bump the pin when
# cutting a new diffwarden release. koopa opts out via MISE_DISABLE_TOOLS
# (hosts/koopa.env.zsh) to keep its pnpm-linked dev build first on PATH.
"npm:diffwarden" = "0.5.0"
# Pinned (same minimum_release_age reason as trunkyard); ~/code/pi-setup
# requires ^0.84.1. Drop the pin once "latest" catches up past 0.84.1.
"npm:@earendil-works/pi-coding-agent" = "0.84.1"
# Prime Agent is distributed as an npm tarball on GitHub rather than npmjs.
# Keep the launch release pinned while mise's minimum_release_age hides it;
# the postinstall hydrates the tarball's runtime dependencies inside mise.
"github:PrimeIntellect-ai/prime-agent" = { version = "v0.7.0", asset_pattern = "prime-agent-0.7.0.tgz", checksum = "sha256:88b6578518c72cd51a825bc80f28e0fef9a64c67de4a7d6fd7afd7ca1b34da0b", strip_components = 1, bin_path = "dist/bundle", rename_exe = { "cli.js" = "prime-agent" }, depends = ["node"], postinstall = "cd \"$MISE_TOOL_INSTALL_PATH\" && mise exec node@24 -- npm install --omit=dev --no-fund --no-audit --ignore-scripts=false" }
"npm:agent-browser" = "latest"
"npm:browse" = "latest"
"npm:portless" = "latest"
"npm:hunkdiff" = "latest"
"npm:vercel" = "latest"
# Linear CLI, for pi only — pi has no MCP, so it drives Linear through a CLI
# plus a skill. Every other agent uses the official Linear MCP instead.
"npm:linearis" = "latest"

# Claude Code is managed separately. The current upstream CLI ships native
# install/update flows (`claude install`, `claude update`) rather than staying
# fully self-contained as a plain npm tool under mise.

# Python CLIs
"pipx:basedpyright" = "latest"
"pipx:beautysh" = "latest"
"pipx:httpie" = "latest"
"pipx:pytest" = { version = "latest", uvx_args = "--with pytest-cov" }
"pipx:ranger-fm" = "latest"
"pipx:ruff" = "latest"
"pipx:youtube-transcript-api" = "latest"
"pipx:antoniorodr/memo" = { version = "latest", os = ["macos"] }
"pipx:jwmoss/notesctl" = { version = "latest", os = ["macos"] }
python = "latest"

# Ruby CLIs
"gem:cocoapods" = "latest"
"gem:fastlane" = "latest"
"github:aurokin/atlassian-cli" = "v0.1.3"
