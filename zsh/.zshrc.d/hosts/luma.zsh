# bun completions and PATH handled by mise

# Portless defaults for this always-home Mac. PORTLESS_LAN=1 makes manual proxy
# starts default to mDNS/LAN mode; the launchd service is installed in LAN mode
# by dot_scripts/portless_service_install.sh.
export PORTLESS_LAN=1
